@model ProductViewModel
@{
    ViewData["Title"] = Model.ResearchProduct.Title;
}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-6">

            @if (Model != null)
            {
                <h1 class="mb-3">@Model.ResearchProduct.Title</h1>

                <div class="d-flex justify-content-between mb-3">
                    <span class="text-secondary">Ngày đăng bài: @Model.ResearchProduct.CreatedAt.ToShortDateString()</span>
                </div>
                <img src="@Model.ResearchProduct.CoverImg" class="d-block img-fluid mb-3" />

                <div class="mb-3 blog-details-content">
                    @Html.Raw(Model.ResearchProduct.Content)
                </div>

            }
            else
            {
                <p>Blog post not found!</p>
            }
        </div>
    </div>
</div>
